---
import AboutHeroImage from '../assets/blog-placeholder-about.jpg';
import { Image } from 'astro:assets';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';

const pageTitle = 'About — Hacker Ethos';
const pageDescription = 'Builder mindset, hacker ethos, and what I am working on.';
---

<html lang="en">
	<head>
		<BaseHead title={pageTitle} description={pageDescription} />
		<style>
			.about-shell {
				width: 720px;
				max-width: calc(100% - 2em);
				margin: 0 auto;
			}
			.about-hero {
				position: relative;
				margin-bottom: 1rem;
				border-radius: 12px;
				overflow: hidden;
				border: 1px solid rgba(130, 198, 230, 0.32);
				box-shadow:
					0 0 0 1px rgba(160, 224, 252, 0.12),
					0 0 28px rgba(10, 24, 40, 0.42);
			}
			.about-hero img {
				display: block;
				width: 100%;
				filter: brightness(0.82) saturate(0.9);
				opacity: 0.86;
			}
			.about-hero-tag {
				position: absolute;
				top: 0.8rem;
				left: 0.8rem;
				padding: 0.25rem 0.55rem;
				border: 1px solid rgba(142, 214, 246, 0.44);
				border-radius: 999px;
				font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
				font-size: 0.62rem;
				letter-spacing: 0.13em;
				text-transform: uppercase;
				color: rgba(194, 238, 255, 0.92);
				background: rgba(8, 18, 30, 0.72);
			}
			.about-manifest {
				margin: 1rem 0 1.4rem;
				padding: 0.75rem 0.9rem;
				border: 1px solid rgba(126, 194, 228, 0.24);
				border-radius: 8px;
				background: rgba(8, 18, 30, 0.58);
			}
			.about-manifest strong {
				display: block;
				margin-bottom: 0.45rem;
				font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
				font-size: 0.72rem;
				letter-spacing: 0.1em;
				text-transform: uppercase;
				color: rgba(170, 230, 252, 0.9);
			}
			.about-manifest ul {
				margin: 0;
				padding-left: 1.1rem;
			}
			.about-manifest li {
				margin: 0.35rem 0;
			}
		</style>
	</head>

	<body class="mesh-page about-page">
		<div class="mesh-bg" aria-hidden="true">
			<div class="mesh-glow mesh-glow-shift"></div>
			<div class="mesh-haze" aria-hidden="true"></div>
			<div class="mesh-vignette" aria-hidden="true"></div>
			<div class="mesh-stripe"></div>
			<div class="mesh-noise" aria-hidden="true"></div>
			<div class="mesh-hex-grid" aria-hidden="true"></div>
		</div>
		<Header />
		<main class="mesh-content">
			<article class="mesh-article about-shell">
				<div class="about-hero">
					<Image width={1020} height={510} src={AboutHeroImage} alt="About cover" />
					<div class="about-hero-tag">hacker profile</div>
				</div>
				<div class="prose">
					<div class="title mesh-title">
						<div class="mesh-meta-terminal">$ profile booted | mode: builder</div>
						<h1 class="mesh-title-text">About — Hacker Ethos</h1>
						<hr />
					</div>

					<p>
						I build products the same way I debug systems: start from first principles,
						ship a thin version fast, then iterate with real feedback.
					</p>

					<div class="about-manifest">
						<strong>Hacker Manifest</strong>
						<ul>
							<li>Curiosity over certainty: question assumptions before scaling complexity.</li>
							<li>Craft over noise: clean interfaces, predictable behavior, readable code.</li>
							<li>Autonomy with accountability: own outcomes, not just commits.</li>
							<li>Open collaboration: document decisions so teams can move fast together.</li>
						</ul>
					</div>

					<h2>What I Build</h2>
					<p>
						I focus on pragmatic web systems: content platforms, AI-first interfaces, and
						design-heavy experiences that still prioritize performance and accessibility.
					</p>

					<h2>How I Work</h2>
					<p>
						I prefer small, testable slices. Every feature should answer three questions:
						what user outcome it improves, what tradeoff it introduces, and how we measure success.
					</p>

					<blockquote>
						Ship small. Learn fast. Keep the system understandable.
					</blockquote>

					<h2>Now</h2>
					<p>
						Currently exploring cyberpunk-flavored theme systems for Astro,
						with configurable visual modes and publish-ready defaults.
					</p>
				</div>
			</article>
		</main>
		<Footer />
	</body>
</html>
