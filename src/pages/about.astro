---
import AboutHeroImage from '../assets/blog-placeholder-about.jpg';
import { Image } from 'astro:assets';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';

const pageTitle = 'About — Hacker Ethos';
const pageDescription = 'Who I am, what I build, and the hacker ethos behind my work.';
---

<html lang="en">
	<head>
		<BaseHead title={pageTitle} description={pageDescription} />
		<style>
			.about-shell {
				width: 720px;
				max-width: calc(100% - 2em);
				margin: 0 auto 2rem;
			}
			.about-hero {
				position: relative;
				margin: 0 auto 1rem;
				border-radius: 12px;
				overflow: hidden;
				border: 1px solid rgba(130, 198, 230, 0.32);
				box-shadow:
					0 0 0 1px rgba(160, 224, 252, 0.12),
					0 0 28px rgba(10, 24, 40, 0.42);
			}
			.about-hero img {
				display: block;
				width: 100%;
				filter: brightness(0.8) saturate(0.9);
				opacity: 0.88;
			}
			.about-hero-tag {
				position: absolute;
				top: 0.75rem;
				left: 0.75rem;
				padding: 0.26rem 0.6rem;
				border: 1px solid rgba(142, 214, 246, 0.44);
				border-radius: 999px;
				font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
				font-size: 0.62rem;
				letter-spacing: 0.13em;
				text-transform: uppercase;
				color: rgba(194, 238, 255, 0.94);
				background: rgba(8, 18, 30, 0.72);
			}
			.about-hero-status {
				display: flex;
				justify-content: space-between;
				gap: 0.6rem;
				padding: 0.25rem 0.55rem;
				border-top: 1px solid rgba(142, 214, 246, 0.26);
				background: linear-gradient(90deg, rgba(10, 22, 38, 0.72), rgba(8, 16, 28, 0.78));
				font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
				font-size: 0.58rem;
				letter-spacing: 0.12em;
				text-transform: uppercase;
				color: rgba(186, 230, 252, 0.84);
			}
			.about-title {
				margin-bottom: 1.4rem;
			}
			.about-section-title {
				font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
				font-size: 0.78rem;
				letter-spacing: 0.12em;
				text-transform: uppercase;
				color: rgba(172, 228, 252, 0.88);
				margin: 1.7rem 0 0.45rem;
			}
			.about-manifest {
				margin: 1rem 0 1.4rem;
				padding: 0.8rem 0.95rem;
				border: 1px solid rgba(126, 194, 228, 0.24);
				border-radius: 8px;
				background: rgba(8, 18, 30, 0.58);
			}
			.about-manifest ul {
				margin: 0;
				padding-left: 1.1rem;
			}
			.about-manifest li {
				margin: 0.38rem 0;
			}
			.about-signature {
				margin-top: 1.2rem;
				font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
				font-size: 0.8rem;
				color: rgba(162, 224, 250, 0.88);
			}
		</style>
	</head>

	<body class="mesh-page about-page">
		<div class="mesh-bg" aria-hidden="true">
			<div class="mesh-glow mesh-glow-shift"></div>
			<div class="mesh-haze" aria-hidden="true"></div>
			<div class="mesh-vignette" aria-hidden="true"></div>
			<div class="mesh-stripe"></div>
			<div class="mesh-noise" aria-hidden="true"></div>
			<div class="mesh-hex-grid" aria-hidden="true"></div>
		</div>
		<Header />
		<div class="mesh-read-progress" aria-hidden="true"></div>
		<main class="mesh-content">
			<div class="mesh-load-scan" aria-hidden="true"></div>
			<article class="mesh-article about-shell">
				<div class="about-hero">
					<Image width={1020} height={510} src={AboutHeroImage} alt="About cover" />
					<div class="about-hero-tag">hacker profile</div>
					<div class="about-hero-status" aria-hidden="true">
						<span>open source active</span>
						<span>build mode</span>
					</div>
				</div>

				<div class="prose">
					<div class="title mesh-title about-title">
						<div class="mesh-meta-terminal">$ profile booted | mode: builder</div>
						<h1 class="mesh-title-text">About — Hacker Ethos</h1>
						<hr />
					</div>

					<h2 class="about-section-title">01 / Who I Am</h2>
					<p>
						I am a product-minded engineer who treats software as craft: clear intent,
						fast iteration, and accountable execution.
					</p>

					<h2 class="about-section-title">02 / What I Build</h2>
					<p>
						I build web systems with Astro, TypeScript, and modern CSS, focused on
						content platforms, interface quality, and AI-era user experiences that remain
						performant and understandable.
					</p>

					<h2 class="about-section-title">03 / Hacker Ethos</h2>
					<div class="about-manifest">
						<ul>
							<li>Curiosity before certainty: test assumptions early.</li>
							<li>Craft before noise: ship clean interfaces and readable systems.</li>
							<li>Open collaboration: document decisions so teams can move together.</li>
							<li>Autonomy with accountability: own outcomes, not just output.</li>
						</ul>
					</div>

					<h2 class="about-section-title">04 / Now</h2>
					<p>
						Currently building a publish-ready Astro cyberpunk theme with configurable
						visual modes, stronger accessibility defaults, and reusable page variants.
					</p>

					<h2 class="about-section-title">05 / Contact</h2>
					<p>
						Open to collaboration on frontend architecture, theme systems, and product UX.
						Reach me via <a href="mailto:hello@example.com">email</a> or connect on
						<a href="https://github.com" target="_blank" rel="noopener noreferrer">GitHub</a>.
					</p>

					<blockquote class="about-signature">&gt; ship small, learn fast</blockquote>
				</div>
			</article>
		</main>
		<Footer />
		<script is:inline>
			(function() {
				function init() {
					var progress = document.querySelector('.mesh-read-progress');
					if (!progress) return;
					function onScroll() {
						var scrollTop = window.scrollY || document.documentElement.scrollTop;
						var scrollHeight = document.documentElement.scrollHeight - window.innerHeight;
						var p = scrollHeight > 0 ? Math.min(1, scrollTop / scrollHeight) : 1;
						progress.style.setProperty('--read-progress', String(p));
					}
					onScroll();
					window.addEventListener('scroll', onScroll, { passive: true });
				}
				if (document.readyState === 'loading') {
					document.addEventListener('DOMContentLoaded', init);
				} else {
					init();
				}
			})();
		</script>
	</body>
</html>
